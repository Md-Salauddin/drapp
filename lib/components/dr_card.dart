import 'package:flutter/material.dart';

class DrCard extends StatelessWidget {
  const DrCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Padding(padding: EdgeInsets.only(bottom: 16));
  }
}
